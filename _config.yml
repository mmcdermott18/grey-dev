defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

plugins:
  - jekyll-sitemap
  - jekyll-seo-tag

domain: mmcdermott18.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://mmcdermott18.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /grey-dev/      # place folder name if the site is served in a subfolder