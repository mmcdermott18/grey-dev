defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

plugins:
  - jekyll-sitemap
  - jekyll-seo-tag

# Site settings
title: GREY
description: 'GREY Architectural Studio' # this means to ignore newlines until "baseurl:"